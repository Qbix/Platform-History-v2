/**
 * Assets model
 * @module Assets
 * @main Assets
 */
var Q = require('Q');

/**
 * Static methods for the Assets model
 * @class Assets
 * @extends Base.Assets
 * @static
 */
var Assets = module.exports;
Q.require('Base/Assets').apply(Assets);

/*
 * This is where you would place all the static methods for the models,
 * the ones that don't strongly pertain to a particular row or table.
 * Just assign them as methods of the Assets object.
 
 * * * */

/* * * */