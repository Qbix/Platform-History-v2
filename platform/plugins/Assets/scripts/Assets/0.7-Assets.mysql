ALTER TABLE `{$prefix}charge`
CHANGE `subscriptionPublisherId` `publisherId` varbinary(255) NOT NULL DEFAULT '' COMMENT 'publisherId of the stream regarding which the charge was made',
CHANGE `subscriptionStreamName` `streamName` varbinary(255) NOT NULL DEFAULT '' COMMENT 'name of the stream regarding which the charge was made';