ALTER TABLE {{prefix}}access
ADD COLUMN `ofParticipantRole` VARCHAR(63) AFTER `ofContactLabel`,
ADD COLUMN `arrivedRatio` DECIMAL(10,4) NOT NULL AFTER `leftCount`,
ADD COLUMN `joinedRatio` DECIMAL(10,4) NOT NULL AFTER `arrivedRatio`,
ADD INDEX byInvitedCount (invitedCount),
ADD INDEX byArrivedRatio (arrivedRatio),
ADD INDEX byInvitedRatio (joinedRatio);