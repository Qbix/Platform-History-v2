ALTER TABLE {$prefix}stream
DROP COLUMN participantCount,
ADD COLUMN participantCounts varchar(255) NOT NULL DEFAULT '[0, 0, 0]' COMMENT 'JSON array of counts for each state [invited, participating, left]' AFTER messageCount;